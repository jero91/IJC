---
title: "sinergiaLinfoma"
author: "Jeronimo Parra"
date: "7/12/2023"
output: html_document
---

## Estudia sinergia en linfoma 

# Onconova + ibrutinib


#PAsos iniciales: Partimos de los fastq, los contamos con salmon y obtenemos los archivos quant.sf que importamos con txi y obtenemos los counts

# 1. Creamos objeto "IDs" con nombres de genes y transritos para mapear con libreria Biomart.

```{r biomart, include=FALSE}


countsFinal <- read.csv("/Users/jparra/Documents/Gael/BTKSinergy/countsFinalAll.csv", sep = ",")
meta <- read.csv("/Users/jparra/Documents/Gael/BTKSinergy/metadata_onconova.csv", row.names = 1)

library(biomaRt)
biomartCacheClear()

mart <- useMart(biomart = "ensembl", dataset = "hsapiens_gene_ensembl")

x <- strsplit(rownames(countsFinal), ".", fixed=T)

y<- c()
for (i in 1:194142) {
  y[i] <- x[[i]][1]
}

#listAttributes(mart)
IDs <- getBM(attributes = c("ensembl_transcript_id","hgnc_symbol","hgnc_id","transcript_is_canonical","definition_1006"),
                 filters = "ensembl_transcript_id", values = y,mart = mart, useCache = FALSE) 
# Obtengo duplicados, si da eror de que no puede abrir el docuemnto temporal es por la cache(, useCache = FALSE).
#Si sal ele error " cannot open compressed file" tenemos que reinicir la sesion de R.
#IDs <- getBM(attributes = c("ensembl_transcript_id"),
                 #filters = "ensembl_transcript_id", values = y,mart = mart)


#FUNCION power.compare.logFC
power.compare.logFC <- function( sig1, sig2, N, N_other = c(2,4,6,8,10), alpha = 0.05, n_tests = 20000){
  d <- seq(0, 3, length.out=1000)
  alpha_multiple <- alpha / n_tests
  df <- lapply( N_other/N, function(n_scale){
    sigSq <- (sig1^2 + sig2^2) / n_scale
    cutoff <- qnorm( alpha_multiple/2, 0, sd = sqrt(sigSq), lower.tail = FALSE)
    p1 <- pnorm(-1*cutoff, d, sqrt(sigSq))
    p2 <- 1-pnorm(cutoff, d, sqrt(sigSq))
    data.frame(n_scale, d, power=p1+p2)
  })
  df <- do.call("rbind", df)
  ggplot(df, aes(d, power, color = as.factor(n_scale*N))) + 
    geom_line() + 
    theme_bw(14) + 
    theme(aspect.ratio = 1, plot.title = element_text(hjust = 0.5)) + 
    ylim(0, 1) + 
    scale_color_discrete("Samples") + 
    xlab(bquote(abs(logFC[observed] - logFC[expected]))) + 
    ggtitle("Power versus difference in logFC")
}

```

# 2. Sinergia

```{r sinergia, echo=T}

samplesNames <- c("UPN1","UPN2","UPN3","UPN4","UPN5","UPN6","UPNRES1","UPNRES2","UPNRES3","UPNRES4","UPNRES5","UPNRES6")

counts  <-   countsFinal[,match(samplesNames ,colnames(countsFinal))]

#Empezamos con este numero de trascritos y muestras:
dim(counts)

meta <- read.csv("/Users/jparra/Documents/Gael/BTKSinergy/metadata_onconova.csv", row.names = 1)

metaSub  <-   meta[match(samplesNames ,meta$ID),]
row.names(metaSub) <- metaSub$ID
metaSub <- metaSub[match(colnames(counts), row.names(metaSub)), ]

#6
library(edgeR)
plot(cpm(counts)[, 1], counts[, 1], ylim = c(0, 50), xlim = c(0, 3))
abline(h = 10, col = "red")
abline(v = 0.25, col = "red")


keep <- rowSums(cpm(counts[]) > 0.25) >= 4
#keep <- rowSums(cpm(counts[]) > 0.25) >= 4

#keep <- rowSums(counts(dds)) > 1

gExpr <- counts[keep, ]

#Tras eliminar los transcritos que tienen menos de 0.15 en mas de 4 muestras nos quedamos con:
dim(gExpr)


#7
y <- DGEList(gExpr)
y <- calcNormFactors(y)


#8

#BiocManager::install("chroGPS")
#library(chroGPS)
#install.packages("mds")


library(stats)
plotMDS(y)

#Añadir PCA. UPNRES5 outlier?

#9
#Vemos que las muestras agrupan por linea celular por lo que añadimos al diseño como covariable.

design <- model.matrix(~ 0 + TREATMENT  + CELL.LINE, metaSub) #B

#ajustamos nombres de las columnas
colnames(design)
colnames(design) <- gsub("\\+", "_", colnames(design))
colnames(design) <- gsub("-", "", colnames(design))
colnames(design) <- gsub("TREATMENT", "", colnames(design))
colnames(design)



#10
#The voom() function prepares the data for linear modeling by converting counts to logCPM and computing weights for heteroscedasticity adjustment. It also creates a diagnostic plot for the mean-variance trend (Fig. 5C). Visually inspect it for fit. Increase the cutoff for lowly expressed genes to improve fit if necessary.
v <- voom(y, design, plot = TRUE, save.plot = TRUE)
#Un poco diferente al del articulo.


#11 fit model

fit <- lmFit(v, design)



#12

colnames(design)

#formula1
cont.matrix <- makeContrasts(
  Ibru = IBRUTINIB - CONTROL,
  Onco = ON12300 - CONTROL,
  Additive = IBRUTINIB + ON12300 - 2*CONTROL,
  Combinatorial = ON12300_IBRUTINIB - CONTROL,
  Synergy = ON12300_IBRUTINIB - IBRUTINIB - ON12300  + 1*CONTROL,
  levels = design)


#13

cont.p <- t(cont.matrix)

#install.packages("GiNA")
#install.packages("EBImage")
#library(GiNA)
library(pheatmap)

h <- pheatmap(cont.p,
              display_numbers = T, number_format = "%.0f",
              breaks = seq(-3, 1, by = 0.5),
              cluster_cols = F, cluster_rows = F)



#14

fit.cont <- contrasts.fit(fit, cont.matrix)


#15

fit.cont <- eBayes(fit.cont)
plotSA(fit.cont, main = "Final model: Mean-variance trend", ylab = "Sqrt( standard deviation )")

summa.fit = decideTests(fit.cont, adjust.method = "fdr")


#mapeo
x <- strsplit(rownames(fit.cont), ".", fixed=T)
nombreGenes<- c()
for (i in 1:dim(gExpr)[[1]]) {
  nombreGenes[i] <- x[[i]][1]
}
fit.cont$genes <- IDs$hgnc_symbol[match(nombreGenes, IDs$ensembl_transcript_id) ]

par(mfrow = c(1, 2))
for (i in 1:length(colnames(fit.cont$contrasts))){
  plotMD(fit.cont, coef = i, status = summa.fit[, i], values = c(-1, 1))
  volcanoplot(fit.cont, coef = i, highlight = 100, 
              names = fit.cont$genes)
}



#16
res.list <- list()
for (i in 1:length(colnames(fit.cont$contrasts))){
  x <- topTable(fit.cont, coef = i, sort.by = "p", n = Inf, confint = T)
  res.list[[i]] <- x
  names(res.list)[i] <- colnames(fit.cont$contrasts)[i]
}










```


#3. OBTENEMOS ranks para GSEA

```{r ranks, echo=T}



ordered <- res.list[[5]] 


x <- strsplit(rownames(ordered), ".", fixed=T)

y<- c()
for (i in 1:dim(ordered)[[1]]) {
  #print(i)
  y[i] <- x[[i]][1]
}


ordered$ID <-y

ranks <- cbind(ordered$t, ordered$ID,ordered$logFC,ordered$P.Value)

ranks<- as.data.frame(ranks)
ranks$V3 <- as.numeric(ranks$V3)
ranks$V1 <- as.numeric(ranks$V1)
ranks <- na.omit(ranks)

ranks$V5 <- IDs$hgnc_symbol[match(ranks$V2,IDs$ensembl_transcript_id)]


#filtrando por p value de sinergia.

#ranks0.05 <- ranks[ranks$V4 < 0.05,]
ranks0.05 <- ranks

ranks0.05 <- ranks0.05[ranks0.05$V4 < 0.05,]

ranks0.05 <- ranks0.05[!ranks0.05$V5 %in% "",]

#ranks0.05 <-as.data.frame(cbind(ranks0.05$V5, ranks0.05$V1))   #t

ranks0.05 <-as.data.frame(cbind(ranks0.05$V2, ranks0.05$V1))   #t

#Guardamos el objeto ranks que contiene la siguiente cantidad de genes: 
dim(ranks0.05)
write.table(ranks0.05, file="/Users/jparra/Documents/Gael/BTKSinergy/ranksSinergyUPN05oncoTranscript.rnk", sep='\t', row.names=F, col.names=F)


#Resultado GSEA con biocarta,kegg,reactome,BP,CC,hallmark: file:///C:/Users/jparra/gsea_home/output/jul18/my_analysis.GseaPreranked.1689786705901/index.html



```






#4. heatmaps aditivo vs combinacion

```{r heatmaps, echo=T}



res.list$Ibru$ID <- rownames(res.list$Ibru)
res.list$Onco$ID <- rownames(res.list$Onco)

res.list$Additive$ID <- rownames(res.list$Additive)
res.list$Combinatorial$ID <- rownames(res.list$Combinatorial)
res.list$Synergy$ID <- rownames(res.list$Synergy)

colnames(res.list$Additive)
log2FC.matrix <- Reduce(function(x,y) merge(x,y,by="ID", all = F),  list(res.list$Ibru[,c("ID","logFC","t","P.Value")], res.list$Onco[,c("ID","logFC","t","P.Value")], res.list$Additive[,c("ID","logFC","t","P.Value")],                 res.list$Combinatorial[,c("ID","logFC","t","P.Value")],
res.list$Synergy[,c("ID","logFC","t","P.Value")]
))





colnames(log2FC.matrix)
## [1] "ensembl"   "Gene_name" "logFC.x"   "P.Value.x" "logFC.y"   "P.Value.y"
## [7] "logFC"     "P.Value"


colnames(log2FC.matrix) <- c("ID","Onco.logFC", "Onco.t" ,"Onco.pvalue","Ibru.logFC", "Ibru.t" ,"Ibru.pvalue","Additive.logFC", "Additive.t" ,"Additive.pvalue","Combinatorial.logFC", "Combinatorial.t", "Combinatorial.pvalue","Synergistic.logFC", "Synergistic.t", "Synergistic.pvalue")


#FCMergeUp <- FCMerge[FCMerge$Fold_Change.x > 0 & FCMerge$Fold_Change.y > 0 ,]




#Add a column assigning synergistic categories to each gene using the categorize.synergy() function, which takes the previously created matrix and the expression cutoff into account.
categorize.synergy <- function(logFCmatrix, meanSE){
  m <- logFCmatrix
  m$magnitude.syn <- NA
  for (i in 1:length(m$ID)){
    if (m$Synergistic.logFC[i] > meanSE){
      if (m$Additive.logFC[i] < -meanSE){
        if (m$Combinatorial.logFC[i] > meanSE){
          m$magnitude.syn[i] = "more.up"
        } else m$magnitude.syn[i] = "less.down"
      } else m$magnitude.syn[i] = "more.up"
    }
    else if (m$Synergistic.logFC[i] < -meanSE){
      if (m$Additive.logFC[i] > meanSE){
        if (m$Combinatorial.logFC[i] < -meanSE){
          m$magnitude.syn[i] = "more.down"
        } else m$magnitude.syn[i] = "less.up"
      } else m$magnitude.syn[i] = "more.down"
    } else m$magnitude.syn[i] = "same"
  }
  m$magnitude.syn <- as.factor(m$magnitude.syn)
  return(m)
}

SE <- sqrt(fit.cont$s2.post) * fit.cont$stdev.unscaled
meanSE = mean(SE[,c(1,2,3,4,5)])

log2FC.matrix <- categorize.synergy(log2FC.matrix, meanSE)


#mapeo

rownames(log2FC.matrix) <- log2FC.matrix$ID
x <- strsplit(rownames(fit.cont), ".", fixed=T)

y<- c()
for (i in 1:dim(gExpr)[[1]]) {
  y[i] <- x[[i]][1]
}
log2FC.matrix$ID <- IDs$hgnc_symbol[match(y, IDs$ensembl_transcript_id) ]

log2FC.matrix$description <- IDs$definition_1006[match(y, IDs$ensembl_transcript_id) ]


write.table(log2FC.matrix, file="/Users/jparra/Documents/Gael/BTKSinergy/log2FCRNA.csv", sep=',', row.names=T, col.names=T)


log2FC.matrixSigni <- log2FC.matrix[log2FC.matrix$Synergistic.pvalue < 0.05,]

#log2FC.matrixSigni <- log2FC.matrixSigni[log2FC.matrixSigni$Additive.pvalue < 0.05,]
#log2FC.matrixSigni <- log2FC.matrixSigni[log2FC.matrixSigni$Combinatorial.pvalue < 0.05,]

x <- strsplit(log2FC.matrixSigni$ID, ".", fixed=T)
y<- c()
for (i in 1:dim(log2FC.matrixSigni)[[1]]) {
  y[i] <- x[[i]][1]
}




#Creamos tabla con descripcion genes
#log2FC.matrix

x <- strsplit(rownames(log2FC.matrix), ".", fixed=T)





log2FC.matrixSigni <- log2FC.matrixSigni[!duplicated(log2FC.matrixSigni$ID),]
log2FC.matrixSigni <- log2FC.matrixSigni[!log2FC.matrixSigni$ID %in% "",]
#rownames(log2FC.matrixSigni) <- log2FC.matrixSigni$ID


library(GiNA)
for (i in 1:length(levels(log2FC.matrixSigni$magnitude.syn))){
  breaks <- c(seq(-6, -0.3,by=0.1),seq(0.3, 6,by=0.1))
  breaks <- append(breaks, -9,0)
  breaks <- append(breaks, 9)
  tmp <- log2FC.matrixSigni[log2FC.matrixSigni$magnitude.syn == 
                         levels(log2FC.matrixSigni$magnitude.syn)[i],
                       c("Additive.logFC","Combinatorial.logFC")]
  h <- pheatmap(tmp,
                #kmeans_k = 30, 
                #annotation_row = rownames(tmp),
                #cellwidth = 70, cellheight = 5,
                border_color = NA, 
                breaks=breaks,
                cluster_cols = F,
                show_rownames = T,
                color = colorRampPalette(rev(brewer.pal(n=9, name="RdBu")))(117),
                main = paste0("logFC expected vs. measured:\n",
                              levels(log2FC.matrixSigni$magnitude.syn)[i]))
  print(h)
}




genes.per.category<- as.data.frame(table(log2FC.matrixSigni$magnitude.syn))
print(genes.per.category)

library(ggplot2)
library(wesanderson)
zissou <- wes_palette("Zissou1", 6, type = "continuous")

q <- ggplot(genes.per.category, aes(x = "", y = Freq, fill = Var1)) +
  geom_col() +
  coord_polar("y", start=0) + 
  scale_fill_manual(values=zissou) +
  theme_void()
#pdf(paste0("results/", experiment.title, "-8_synergy-categories_pie-chart.pdf"))
print(q)

#write.table(log2FC.matrixSigni, file="/Users/jparra/Documents/Gael/BTKSinergy/genesSinergiaDescriptionOncoIbru.csv", sep=',', row.names=T, col.names=T)





#Synergy score:

library(qvalue)
synergy.pvalues <- res.list$Synergy$P.Value
pi1 <- 1 - qvalue(synergy.pvalues)$pi0
print(pi1)

plot.new()+
text(0.4,0.75,labels=paste0("\n",round(pi1 * 100, 2), 
                            "% non-null \np-values and \n", 
                            round(sum(res.list$Synergy$P.Value < 0.1) *
                                    100/length(res.list$Synergy$ID), 2), 
                            " % of genes with \nsynergy FDR < 0.1"))


FDR <- p.adjust(res.list$Synergy$P.Value, method="BH")

hist(synergy.pvalues)



#synergy power
SE <- sqrt(fit.cont$s2.post) * fit.cont$stdev.unscaled

sig1 <- median(SE[,"Additive"])
sig2 <- median(SE[,"Combinatorial"])
g <- power.compare.logFC(sig1, sig2, N = 4, N_other = c(4, 6, 8, 10, 14,20,27), 
                         alpha = 0.05, n_tests = 20000)
print(g)




#Representamos genes en heatmap combi vs aditive:


#genesG2<- c("PSIP1","ASF1B","DUT","POLD1","DSCC1","GINS1","AURKB","E2F8","TFRC","EED","DONSON")





#El transcrito con p.valor de sinergia ma significativo es :ENST00000330280.11. y el menos ENST00000253457.8

MasSigni <- c("ENST00000330280.11","ENST00000414849.6")
countsMRNA <- counts[rownames(counts) %in% MasSigni,]

pheatmap(countsMRNA,cluster_cols = F)


# 2 +4 deberian ser mas que 5 pero no termino de verlo.




#La mas significativa de ocntrol vs onconova si se ve en le counts la tendencia?


onco <- res.list$Onco


# mas signi ENST00000326004.4

MasSigni <- c("ENST00000330280.11","ENST00000326004.4")

countsMRNA <- counts[rownames(counts) %in% MasSigni,]

pheatmap(countsMRNA,cluster_cols = F)


#onco conmpara muestra onconova 2  vs control 1 
#En las 2 se ve la tendencia de menos expresion se mantiene contante en las 2 tipos de muestras.
#Si la sinergia funciona deberia de verse en los heatmap?


#TP53
MasSigni <- rownames(log2FC.matrix)[log2FC.matrix$ID %in% "TP53" ]


countsMRNA <- counts[rownames(counts) %in% MasSigni,]

pheatmap(countsMRNA,cluster_cols = F)


#TP53
MasSigni <- rownames(log2FC.matrix)[log2FC.matrix$ID %in% "USP24" ]


countsMRNA <- counts[rownames(counts) %in% MasSigni,]

pheatmap(countsMRNA,cluster_cols = F)
```









#heatmap del counts de los genes de la ruta E2F en todas o algunas cellines.

```{r heatmap, echo=T}


library(pheatmap)

countsFinal <- read.csv("/Users/jparra/Documents/Gael/BTKSinergy/countsFinalAll.csv", sep = ",")
#meta <- read.csv("/Users/jparra/Documents/Gael/BTKSinergy/metadata_onconova.csv", row.names = 1)

#samplesNames <- c("UPN1","UPN2","UPNRES1","UPNRES2")
#samplesNames <- c("UPN1","UPNRES1","UPN2","UPNRES2")
samplesNames <- c("UPN2","UPNRES2","UPN1","UPNRES1")

counts  <-   countsFinal[,match(samplesNames ,colnames(countsFinal))]


#pheatmap(counts,cluster_cols = F)


#read_tsv from GSEA

library(tidyverse) #read_tsv
#E2F <- read_tsv("/Users/jparra/Documents/Gael/BTKSinergy/GSEAresults/HALLMARK_E2F_TARGETS.tsv")

#E2F <- read.csv("/Users/jparra/Documents/Gael/BTKSinergy/GSEAresults/HALLMARK_E2F_TARGETS.csv", sep = ",")
E2F <- read.csv("/Users/jparra/Downloads/HALLMARK_E2F_TARGETS.v2023.2.Hs.tsv", sep = ",")

#E2FMini <- c("TUBB", "HMGA1")

x <- strsplit(rownames(counts), ".", fixed=T)

y<- c()
for (i in 1:194142) {
  y[i] <- x[[i]][1]
}

#listAttributes(mart)
mart <- useMart(biomart = "ensembl", dataset = "hsapiens_gene_ensembl")

IDs <- getBM(attributes = c("ensembl_transcript_id","hgnc_symbol","hgnc_id","transcript_is_canonical","definition_1006"),
                 filters = "ensembl_transcript_id", values = y,mart = mart, useCache = FALSE)



counts$ID <- IDs$hgnc_symbol[match(y, IDs$ensembl_transcript_id) ]
counts$canonical <- IDs$transcript_is_canonical[match(y, IDs$ensembl_transcript_id) ]

countsCano <- counts[counts$canonical == 1,]
countsCano <- countsCano[!is.na(countsCano$canonical),]

#na.omit(countsCano$canonical)
#countsCano <- countsCano[na.omit(countsCano$canonical) , ]


countsE2F <- countsCano[countsCano$ID %in% E2F[22:420,1],]
#countsE2FMini <- countsCano[countsCano$ID %in% E2FMini,]
#countsE2FMini <- countsE2FMini[(countsE2F$UPN1 != 0), ] 
#countsE2FMini <- countsE2FMini[2:5,]



#rownames(countsE2F) <- countsE2F$ID

#countsE2FMini <- countsE2F[,1:4]

countsE2F <- countsE2F[(countsE2F$UPN1 != 0), ] 


#library(pheatmap)

pheatmap(countsE2F[,1:4],scale = "row",cluster_cols = F,fontsize = 5,labels_row= countsE2F$ID,color = colorRampPalette(c("blue","white", "red"))(20))


#pheatmap(countsE2FMini,cluster_cols = F,cluster_rows = F)

#countsE2F[,1:4]




#countsE2F<- as.matrix(countsE2F)

#pheatmap(countsE2F,scale = "row", clustering_distance_rows = "correlation")



#countsE2F <- countsE2F[(countsE2F$UPN1 != 0 && countsE2F$UPN2 != 0), ] 




#countsE2F <- countsE2F[,1:4]




#countsE2F<- as.matrix(countsE2F)
#countsE2F<- as.numeric(countsE2F)

#pheatmap(countsE2F[,1:4],scale = "none")



#countsE2F <- countsE2F[(countsE2F$ID != "TUBB"), ] 
#countsE2F <- countsE2F[(countsE2F$ID != "TCF19"), ] 
#countsE2F <- countsE2F[(countsE2F$ID != "CDKN2A"), ] 
#countsE2F <- countsE2F[(countsE2F$ID != "UBR7"), ] 
#countsE2F <- countsE2F[(countsE2F$ID != "AK2"), ] 
#countsE2F <- countsE2F[(countsE2F$ID != "UBR7"), ] 
#countsE2F <- countsE2F[(countsE2F$ID != "WDR90"), ] 
#countsE2F <- countsE2F[(countsE2F$ID != "SHMT1"), ] 
#pheatmap(countsE2F[,1:4],scale = "column")



#pheatmap(countsE2F,scale = "row")

#pheatmap(countsE2F[,1:4],scale = "row",cluster_cols = F,)


#pheatmap(countsE2F[,1:4],scale = "row",cluster_cols = F, annotation_row = countsE2F$ID )
#pheatmap(countsE2F[,1:4],scale = "row",cluster_cols = F, annotation_row = rownames(countsE2F) )

#rownames(countsE2F) <- countsE2F$ID


#countsE2F <- countsE2F[!duplicated(countsE2F$ID), ]
#rownames(countsE2F) <- countsE2F$ID
#pheatmap(countsE2F[,1:4],scale = "row",cluster_cols = F,)


#pheatmap(countsE2FMini[,1:4],scale = "row")


#pheatmap(countsE2F,scale = "row", clustering_distance_rows = "correlation")


#pheatmap(countsE2F[,1:4],scale = "row")

#pheatmap(countsE2F[,1:4],cluster_cols = F,cluster_rows = T)




#countsE2F <- countsE2F[(countsE2F$UPN1 != 0 & countsE2F$UPN2 != 0 &  countsE2F$UPNRES1 != 0 & countsE2F$UPNRES2 != 0), ] 
#countsE2F <- countsE2F[(countsE2F$UPN1 != 0), ] 








#todas las lineas celulares

countsFinal <- read.csv("/Users/jparra/Documents/Gael/BTKSinergy/countsFinalAll.csv", sep = ",")

samplesNames <- c("UPN1","UPNRES1","REC1","JEKO1","MINO1","UPN2","UPNRES2","REC2","JEKO2","MINO2")
#samplesNames <- c("UPN1","UPN2","UPNRES1","UPNRES2")

counts  <-   countsFinal[,match(samplesNames ,colnames(countsFinal))]
x <- strsplit(rownames(counts), ".", fixed=T)

y<- c()
for (i in 1:194142) {
  y[i] <- x[[i]][1]
}

#listAttributes(mart)
mart <- useMart(biomart = "ensembl", dataset = "hsapiens_gene_ensembl")

IDs <- getBM(attributes = c("ensembl_transcript_id","hgnc_symbol","hgnc_id","transcript_is_canonical","definition_1006"),
                 filters = "ensembl_transcript_id", values = y,mart = mart, useCache = FALSE)



counts$ID <- IDs$hgnc_symbol[match(y, IDs$ensembl_transcript_id) ]
counts$canonical <- IDs$transcript_is_canonical[match(y, IDs$ensembl_transcript_id) ]

countsCano <- counts[counts$canonical == 1,]
countsCano <- countsCano[!is.na(countsCano$canonical),]


#countsCano <- countsCano[!duplicated(countsCano$ID),]



#na.omit(countsCano$canonical)
#countsCano <- countsCano[na.omit(countsCano$canonical) , ]

E2F <- read.csv("/Users/jparra/Documents/Gael/BTKSinergy/GSEAresults/HALLMARK_E2F_TARGETS.csv", sep = ",")

#countsE2F <- countsCano[countsCano$ID %in% E2F$SYMBOL,]
countsE2F <- countsCano[countsCano$ID %in% E2F[22:420,1],]



#No puedo hacer un heatmap con valores 0, o los quito o le sumo 1 a todos.
countsE2FMini <- countsE2F[(countsE2F$UPN1 != 0), ] 
countsE2FMini <- countsE2FMini[(countsE2FMini$UPNRES1 != 0), ] 
rownames(countsE2FMini)<- countsE2FMini$ID

pheatmap(countsE2FMini[,1:10],scale = "row",cluster_cols = F,fontsize = 5,color = colorRampPalette(c("blue","white", "red"))(20),)

library(pheatmap)

#pheatmap(countsE2F[,1:10],scale = "none")

#pheatmap(countsE2F[,1:10],scale = "row")

#pheatmap(countsE2F[,1:10],cluster_cols = F)




#para sumar 1
#countsE2F1 <- countsE2F[,1:10]+1


#countsE2F1 <- countsE2F1[(rownames(countsE2F1) != "ENST00000376257.8"), ] 

#pheatmap(countsE2F1,scale = "row",cluster_cols = F)


#pheatmap(countsE2F1,scale = "row",cluster_cols = F)










  
  
  
  
  
  
  
 # E2FMini <- c("DLGAP5", "PTTG1", "CDKN3", "LIG1", "CENPE", "HMGB2", "DNMT1", "MKI67", "MXD3")
#countsE2FMini <- countsCano[countsCano$ID %in% E2FMini,]
#rownames(countsE2FMini)<- countsE2FMini$ID
#pheatmap(countsE2FMini[,1:4],scale = "row",cluster_cols = F,fontsize = 5,color = colorRampPalette(c("blue", "white", "red"))(20),)











#heatmap3


library(heatmap3)

#targets <- read.metharray.sheet(dataDirectory, pattern="SampleSheetAutop34.csv")

#heatmap3(countsE2F1, ColSideCut=10, method = "ward.D2") 


#heatmap3(countsE2F1, ColSideCut=10, method = "single") 


#heatmap3(bVals0.15Signi, ColSideCut=10, method = "single")  #"average", "mcquitty", "median" or "centroid".

#heatmap3(bVals0.15Signi, ColSideCut=10, method = "complete") #distfun = function(x){dist(x, method = "euclidean")},     "complete", "average", "mcquitty", "median" or "centroid".


pheatmap(countsE2F[,1:4],scale = "row",cluster_cols = F,fontsize = 5,color = colorRampPalette(c("blue","white", "red"))(20))






#Para quedarme con los realmente canonicos, los que tiene mayor numeor de lecturas
counts$suma <- rowSums(counts[,1:4])

resultado <- counts %>%
  group_by(ID) %>%
  filter(suma == max(suma)) %>%
  ungroup()   #se duplican los que tiene valor 0, deberia eliminarlos.

#resultado <- counts %>%
#  group_by(ID) %>%
#  filter(suma == max(suma) &&  suma != 0 ) %>%
#  ungroup()   #se duplican los que tiene valor 0, deberia eliminarlos.

countsE2F <- resultado[resultado$ID %in% E2F[22:420,1],]



counts$transcript <- rownames(counts)

#countsE2FMini <- countsE2F[(countsE2F$UPN1 != 0), ] 
#countsE2FMini <- countsE2FMini[(countsE2FMini$UPNRES1 != 0), ] 
rownames(countsE2F)<- countsE2F$ID



#subset mejor caracterizan en gsea

E2FMini <- c("HTR3A","RPS29P5","NRG1","ANO2","NGEF","CSRP2","CCDC198")


countsE2F <- resultado[resultado$ID %in% E2FMini,]
countsE2F <- countsE2F[!duplicated(countsE2F$ID),]
rownames(countsE2F)<- countsE2F$ID




#subset mejor caracterizan en heatmap


E2FMini <- c("MXD3","DEK","CENPE","HMGB2","DNMT1","CDKN3","PTTG1","DLGAP5","LIG1","KIF18B","CDCA3","MKT67","SPC24","ORC6","DIAPH3","KIF2C","RAD51AP1","CIT","WDR90" )

countsE2F <- resultado[resultado$ID %in% E2FMini,]

rownames(countsE2F)<- countsE2F$ID


#genes t stadistico
E2FMini <- c("MXD3","PRPF31","ATF6B","CCDC28A","PAICS","GTF2H2")

countsE2F <- resultado[resultado$ID %in% E2FMini,]

rownames(countsE2F)<- countsE2F$ID

#


```


pheatmap(countsE2F[,1:10],scale = "row",cluster_cols = F,fontsize = 5,color = colorRampPalette(c("blue","white", "red"))(20),)

pheatmap(countsE2FMini[,1:10],scale = "row",cluster_cols = F,fontsize = 5,labels_row= countsE2FMini$ID,color = colorRampPalette(c("blue","white", "red"))(20))

pheatmap(countsE2F[,1:4],scale = "row",cluster_cols = F,fontsize = 15,labels_row= countsE2F$ID,color = colorRampPalette(c("blue","white", "red"))(20))


#probando diferentes metodos:
pheatmap(countsE2F[,1:4],scale = "row",cluster_cols = F,fontsize = 5,labels_row= countsE2F$ID, method = "ward.D2" ,color = colorRampPalette(c("blue","white", "red"))(20))

pheatmap(countsE2F[,1:4],scale = "row",cluster_cols = F,fontsize = 5,labels_row= countsE2F$ID, method = "single" ,color = colorRampPalette(c("blue","white", "red"))(20))

pheatmap(countsE2F[,1:4],scale = "row",cluster_cols = F,fontsize = 5,labels_row= countsE2F$ID, method = "complete" ,color = colorRampPalette(c("blue","white", "red"))(20))

pheatmap(countsE2F[,1:4],scale = "row",cluster_cols = F,fontsize = 5,labels_row= countsE2F$ID, method = "average" ,color = colorRampPalette(c("blue","white", "red"))(20))

pheatmap(countsE2F[,1:4],scale = "row",cluster_cols = F,fontsize = 5,labels_row= countsE2F$ID, method = "median" ,color = colorRampPalette(c("blue","white", "red"))(20))

#No cambia nada

counts[counts$ID == "HTR3A",]


heatmap3(countsE2F[,1:4], method == "ward.D2")

library(dplyr)

resultado <- counts %>%
  rowwise() %>%
  mutate(suma = sum(c_across(starts_with("valor")))) %>%
  ungroup() %>%
  group_by(ID) %>%
  slice_max(order_by = suma, with_ties = FALSE) %>%
  select(-suma)

pheatmap(countsE2F[,1:4],scale = "row")
pheatmap(countsE2F[,1:4],cluster_cols = F,cluster_rows = T)



pheatmap(countsE2F[,1:4],scale = "row",cluster_cols = F,fontsize = 5)


pheatmap(countsE2F[,1:4],scale = "row",cluster_cols = F,fontsize = 5,color = colorRampPalette(c("blue", "white", "red"))(20),)

pheatmap(countsE2F[,1:4],scale = "row",cluster_cols = F,fontsize = 5,color = colorRampPalette(c("blue","white", "red"))(20),)




heatmap3(countsE2F1, method = "ward.D2")

heatmap3(countsE2F1, method = "ward.D")

heatmap3(countsE2F1, method = "single")

heatmap3(countsE2F1,  method = "complete")

heatmap3(countsE2F1,  method = "average")

heatmap3(countsE2F1,  method = "mcquitty") 

heatmap3(countsE2F1,  method = "median") 

heatmap3(countsE2F1,  method = "centroid") 



log2FC.matrixUP <- log2FC.matrix[log2FC.matrix$Synergistic.logFC >12 |log2FC.matrix$Synergistic.logFC < -12  ,]
rownames(log2FC.matrixUP) <- log2FC.matrixUP$ID

library(GiNA)



#aqui vemos el efecto antagonico
tmp <- log2FC.matrixUP[,c("Onco.logFC","Ibru.logFC","Additive.logFC","Combinatorial.logFC")]
pheatmap(tmp,
                #kmeans_k = 30, 
                #annotation_row = rownames(tmp),
                #cellwidth = 70, cellheight = 5,
                border_color = NA, 
               # breaks=breaks,
                cluster_cols = F,
                show_rownames = T,
                color = colorRampPalette(rev(brewer.pal(n=9, name="RdBu")))(117),
                main = paste0("logFC expected vs. measured:\n",
                              levels(log2FC.matrixUP$magnitude.syn)[i]))
                              
                    
                    
                    
                              
#aqui vemos el efecto sinergico positivo


log2FC.matrixUP <- log2FC.matrix[log2FC.matrix$Onco.logFC > 0 & log2FC.matrix$Ibru.logFC > 0 ,]

log2FC.matrixUP <- log2FC.matrixUP[log2FC.matrixUP$Synergistic.logFC >2,]
rownames(log2FC.matrixUP) <- log2FC.matrixUP$ID

log2FC.matrixUP <- log2FC.matrixUP[order(log2FC.matrixUP$Synergistic.logFC,decreasing=T),]

         
#comprobar si t stadistico son las mas sinergicas

#log2FC.matrixUP <- log2FC.matrix[log2FC.matrix$Onco.logFC > 0 & log2FC.matrix$Ibru.logFC > 0 ,]
log2FC.matrixT <- log2FC.matrix

log2FC.matrixT                             
x <- as.data.frame(strsplit(log2FC.matrixT$ID, ".", fixed=T))

rownames(log2FC.matrixT)<- x[1,]
log2FC.matrixT$ID  <- IDs$hgnc_symbol[match(rownames(log2FC.matrixT), IDs$ensembl_transcript_id) ]

#t=9
ttest <- c("GTF2H2","NUSAP1")

log2FC.matrixT2 <- log2FC.matrixT[log2FC.matrixT$ID %in% ttest,]

#rownames(countsE2F)<- countsE2F$ID tendria que quitar duplicaods

tmp <- log2FC.matrixT2[,c("Onco.logFC","Ibru.logFC","Additive.logFC","Combinatorial.logFC","Synergistic.logFC")]
pheatmap(tmp,border_color = NA, 
                cluster_cols = F,
                cluster_rows = F,
                show_rownames = T,
                color = colorRampPalette(c("blue","white", "red"))(30),
                main = paste0("logFC expected vs. measured:\n",
                              "UP"))

#cambio nombres
x <- as.data.frame(strsplit(rownames(log2FC.matrixUP), ".", fixed=T))

rownames(log2FC.matrixUP)<- x[1,]

log2FC.matrixUP$ID  <- IDs$hgnc_symbol[match(rownames(log2FC.matrixUP), IDs$ensembl_transcript_id) ]

log2FC.matrixUP <- na.omit(log2FC.matrixUP)

rownames(log2FC.matrixUP) <- log2FC.matrixUP$ID

#heatmap

tmp <- log2FC.matrixUP[,c("Onco.logFC","Ibru.logFC","Additive.logFC","Combinatorial.logFC","Synergistic.logFC")]
pheatmap(tmp,border_color = NA,fontsize = 15, 
                cluster_cols = F,
                cluster_rows = F,
                show_rownames = T,
                color = colorRampPalette(c("white", "red"))(30),
                main = paste0("logFC expected vs. measured:\n",
                              "UP"))

#Comprobamos el mapeo
#IDs[IDs$ensembl_transcript_id == "ENST00000700595",]


#log2FC.matrixDOWN <- na.omit(log2FC.matrixDOWN)

rownames(log2FC.matrixDOWN) <- log2FC.matrixDOWN$ID                 
                 
                              
                              
#aqui vemos el efecto sinergico negativo

 
log2FC.matrixDOWN <- log2FC.matrix[log2FC.matrix$Onco.logFC < 0 & log2FC.matrix$Ibru.logFC < 0 ,]

#cambio nombres
x <- as.data.frame(strsplit(log2FC.matrixDOWN$ID , ".", fixed=T))

rownames(log2FC.matrixDOWN)<- x[1,]

log2FC.matrixDOWN$ID  <- IDs$hgnc_symbol[match(rownames(log2FC.matrixDOWN), IDs$ensembl_transcript_id) ]

#cambio nombres FIN

log2FC.matrixDOWN <- log2FC.matrixDOWN[log2FC.matrixDOWN$Synergistic.logFC < -6,]  # porque pongo este filtro?

log2FC.matrixDOWN <- na.omit(log2FC.matrixDOWN)

rownames(log2FC.matrixDOWN) <- log2FC.matrixDOWN$ID

log2FC.matrixDOWN <- log2FC.matrixDOWN[order(log2FC.matrixDOWN$Synergistic.logFC,decreasing=F),]

tmp <- log2FC.matrixDOWN[,c("Onco.logFC","Ibru.logFC","Additive.logFC","Combinatorial.logFC","Synergistic.logFC")]
pheatmap(tmp,border_color = NA, fontsize = 15, 
                cluster_cols = F,
                cluster_rows = F,
                show_rownames = T,
                color = colorRampPalette(c("blue", "white"))(30),
                main = paste0("logFC expected vs. measured:\n",
                              "UP"))
                                                            

                                                                            
                                                                            
 #Hay mucho efecto anatoganico tengo que introducir la condicion en combinatorial         
 
 
 
 log2FC.matrixDOWN <- log2FC.matrix[log2FC.matrix$Onco.logFC < 0 & log2FC.matrix$Ibru.logFC < 0 & log2FC.matrix$Combinatorial.logFC < 0,]







#Comprobamos el mapeo
#IDs[IDs$ensembl_transcript_id == "ENST00000639899",]


log2FC.matrixDOWN <- na.omit(log2FC.matrixDOWN)

rownames(log2FC.matrixDOWN) <- log2FC.matrixDOWN$ID






#Heatmap E2F


E2FGenes <- "AK2,ANP32E,ASF1A,ASF1B,ATAD2,AURKA,AURKB,BARD1,BIRC5,BRCA1,BRCA2,BRMS1L,BUB1B,CBX5,CCNB2,CCNE1,CCP110,CDC20,CDC25A,CDC25B,CDCA3,CDCA8,CDK1,CDK4,CDKN1A,CDKN1B,CDKN2A,CDKN2C,CDKN3,CENPE,CENPM,CHEK1,CHEK2,CIT,CKS1B,CKS2,CSE1L,CTCF,CTPS1,DCK,DCLRE1B,DCTPP1,DDX39A,DEK,DEPDC1,DIAPH3,DLGAP5,DNMT1,DONSON,DSCC1,DUT,E2F8,EED,EIF2S1,ESPL1,EXOSC8,EZH2,GINS1,GINS3,GINS4,GSPT1,H2AX,H2AZ1,HELLS,HMGA1,HMGB2,HMGB3,HMMR,JPT1,HNRNPD,HUS1,ILF3,ING3,IPO7,KIF18B,KIF22,KIF2C,KIF4A,KPNA2,LBR,LIG1,LMNB1,LUC7L3,LYAR,MAD2L1,MCM2,MCM3,MCM4,MCM5,MCM6,MCM7,MELK,MKI67,MLH1,MMS22L,MRE11,MSH2,MTHFD2,MXD3,MYBL2,MYC,NAA38,NAP1L1,NASP,NBN,NCAPD2,NME1,NOLC1,NOP56,NUDT21,NUP107,NUP153,NUP205,ORC2,ORC6,PA2G4,PAICS,PAN2,PCNA,PDS5B,PHF5A,PLK1,PLK4,PMS2,PNN,POLA2,POLD1,POLD2,POLD3,POLE,POLE4,POP7,PPM1D,PPP1R8,PRDX4,PRIM2,PRKDC,PRPS1,PSIP1,PSMC3IP,PTTG1,RACGAP1,RAD1,RAD21,RAD50,RAD51AP1,RAD51C,RAN,RANBP1,RBBP7,RFC1,RFC2,RFC3,RNASEH2A,RPA1,RPA2,RPA3,CNOT9,RRM2,SHMT1,SLBP,SMC1A,SMC3,SMC4,SMC6,SNRPB,SPAG5,SPC24,SPC25,SRSF1,SRSF2,SSRP1,STAG1,STMN1,SUV39H1,SYNCRIP,TACC3,TBRG4,TCF19,TFRC,TIMELESS,TIPIN,TK1,TMPO,TOP2A,TP53,TRA2B,TRIP13,TUBB,TUBG1,UBE2S,UBE2T,UBR7,UNG,USP1,WDR90,WEE1,XPO1,XRCC6,ZW10"



#E2FGenes <- c("AK2","ANP32E","ASF1A","ASF1B","ATAD2","AURKA","AURKB","BARD1","BIRC5","BRCA1",BRCA2",BRMS1L",BUB1B,CBX5,CCNB2,CCNE1,CCP110,CDC20,CDC25A,CDC25B,CDCA3,CDCA8,CDK1,CDK4,CDKN1A,CDKN1B,CDKN2A,CDKN2C,CDKN3,CENPE,CENPM,CHEK1,CHEK2,CIT,CKS1B,CKS2,CSE1L,CTCF,CTPS1,DCK,DCLRE1B,DCTPP1,DDX39A,DEK,DEPDC1,DIAPH3,DLGAP5,DNMT1,DONSON,DSCC1,DUT,E2F8,EED,EIF2S1,ESPL1,EXOSC8,EZH2,GINS1,GINS3,GINS4,GSPT1,H2AX,H2AZ1,HELLS,HMGA1,HMGB2,HMGB3,HMMR,JPT1,HNRNPD,HUS1,ILF3,ING3,IPO7,KIF18B,KIF22,KIF2C,KIF4A,KPNA2,LBR,LIG1,LMNB1,LUC7L3,LYAR,MAD2L1,MCM2,MCM3,MCM4,MCM5,MCM6,MCM7,MELK,MKI67,MLH1,MMS22L,MRE11,MSH2,MTHFD2,MXD3,MYBL2,MYC,NAA38,NAP1L1,NASP,NBN,NCAPD2,NME1,NOLC1,NOP56,NUDT21,NUP107,NUP153,NUP205,ORC2,ORC6,PA2G4,PAICS,PAN2,PCNA,PDS5B,PHF5A,PLK1,PLK4,PMS2,PNN,POLA2,POLD1,POLD2,POLD3,POLE,POLE4,POP7,PPM1D,PPP1R8,PRDX4,PRIM2,PRKDC,PRPS1,PSIP1,PSMC3IP,PTTG1,RACGAP1,RAD1,RAD21,RAD50,RAD51AP1,RAD51C,RAN,RANBP1,RBBP7,RFC1,RFC2,RFC3,RNASEH2A,RPA1,RPA2,RPA3,CNOT9,RRM2,SHMT1,SLBP,SMC1A,SMC3,SMC4,SMC6,SNRPB,SPAG5,SPC24,SPC25,SRSF1,SRSF2,SSRP1,STAG1,STMN1,SUV39H1,SYNCRIP,TACC3,TBRG4,TCF19,TFRC,TIMELESS,TIPIN,TK1,TMPO,TOP2A,TP53,TRA2B,TRIP13,TUBB,TUBG1,UBE2S,UBE2T,UBR7,UNG,USP1,WDR90,WEE1,XPO1,XRCC6,ZW10)




#library(stringr)

#E2FGenes <- str_replace(',', '","',E2FGenes)
#library(stringr)

#BiocManager::install("stringr",force = TRUE)
#library(chroGPS)
#install.packages("mds")




colnames(design) <- gsub("\\+", "_", colnames(design))

E2FGenes <- gsub(',', '"',E2FGenes)


E2FGenes <- gsub('\\', '',E2FGenes)

gsub("\\(|\\)", "", x)

E2FGenes <- gsub("\\(|\\)", "", E2FGenes)


E2FGenes <- gsub(",", "\",\"", E2FGenes)

E2FGenes <- gsub(',', "\"\\1\"", E2FGenes)

E2FGenes <- gsub("\\\\" ,"", E2FGenes)





texto <- "a,b,c,d"

# Separar los elementos por comas
elementos <- unlist(strsplit(E2FGenes, ","))

# Agregar comillas a cada elemento
elementos_con_comillas <- paste0("\"", elementos, "\"")

# Unir los elementos con comas
resultado <- paste(elementos_con_comillas, collapse = ",")

# Imprimir el resultado
cat(resultado)



E2FMini <- c("TUBB", "HMGA1")


#nombre genes a copunts y listyo
countsCano <- counts[counts$canonical == 1,]
countsCano <- countsCano[!is.na(countsCano$canonical),]

#na.omit(countsCano$canonical)
#countsCano <- countsCano[na.omit(countsCano$canonical) , ]


countsE2F <- countsCano[countsCano$ID %in% E2FMini,]
#countsE2FMini <- countsCano[countsCano$ID %in% E2FMini,]

